#include <stdio.h>
#include <stdlib.h>

#include "librerias.h"

void cargar(int* vector, int j){
    for(int i = 0; i < j; i++){
        printf("\n Ingrese numero: ");
        scanf("%d", vector)
    }
}
